apiVersion: v1
kind: ConfigMap
metadata:
  name: beatstream-config
  namespace: beatstream
data:
  # Non-sensitive endpoint config — safe to store in plaintext
  MINIO_ENDPOINT: "minio:9000"
  MINIO_BUCKET: "beatstream-audio"
  KAFKA_BROKERS: "redpanda:9092"
  PORT: "8080"
  METRICS_PORT: "9090"
